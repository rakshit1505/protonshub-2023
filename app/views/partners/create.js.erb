<% if @partner.errors.present? %>
  alert("<%= @partner.errors.attribute_names.join(', ').titleize + " must exist." %>")
<% else %>
  window.location.href = "<%= ENV['HOST_URL'] %>/thank_you"
<% end %>